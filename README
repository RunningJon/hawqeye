HAWQEYE is a test suite to compare HAWQ (Pivotal HDB) with other SQL on Hadoop products.  The tests include:
1.  Impala TPC-DS

Install:
Download hawqeye.sh from github.  This can't be done with curl because it is a private repo.
chmod 755 hawqeye.sh
./hawqeye.sh

Usage:
Change directory to the test you wish to run and then execute rollout.sh.
Example:
cd impala-tpcds
./rollout.sh
